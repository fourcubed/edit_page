<p id="success"><%= notice %></p>
<%= form_for(@page, :url => url_for(:controller => 'edit_page/pages', :action => 'update', :id => @page.id)) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<% end %>

<p><%= link_to "cancel", url_for(:contoller => 'edit_page/pages', :action => 'show', :id => @page.id) %></p>
<p><%= link_to "Delete", url_for(:controller => 'edit_page/pages', :action => 'destroy', :id => @page.id), :method => :delete, :confirm => "Are you sure you want to delete this page?" %></p>